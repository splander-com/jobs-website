# Public Supabase config for the feedback form. The anon key is publish-safe
# (same key bundled in the extension). The website_feedback table is NOT
# readable by this key — inserts go only through the submit-feedback function.
# Set these in Vercel project env (and locally in website/.env for dev).
PUBLIC_SUPABASE_URL=https://hcwnozzjsgqibhpebkhx.supabase.co
PUBLIC_SUPABASE_ANON_KEY=
